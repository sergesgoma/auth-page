import React from "react";
import "./Button.css";

export const Button = () => {
    return (
      <div>
        <button type="submit" className="btn">
          LOG IN
        </button>
      </div>
    );
}